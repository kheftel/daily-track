extends layout

block content
    .chartholder(data-setid=dataset._id data-setname=dataset.name)
    //- .row.flex-expand
        //- .col


            //- .card.border-light.h-100.shadow-rb
            //-     .card-header.p-2
            //-         .card-title.m-0.d-flex.content-justify-between
            //-             h4.m-0.text-shadow
            //-                 span.align-middle= dataset.name
            //-             button.ml-auto.btn.btn-primary.btn-shadow
            //-                 span.fas.fa-edit
            //-     .card-body(style="position: relative;")
            //-         .chartcontainer(id="set-0-" + dataset._id data-setid=dataset._id)


block pagescript
    script.

        $(".chartholder").each(function(i, container) {
            var setid = container.dataset.setid;
            var controller = new ChartController(container);
            controller.colorOffset = 0;
            controller.addDataset(setid);
        });

        //- window.controller = window.helpers.createChartControllerFromId('mainChart', '#{dataset._id}');



        //- .row
        //-     .col
        //-         h4
        //-             =dataset.name
        //-             a(href="#") 
        //-                 span.fas.fa-edit
