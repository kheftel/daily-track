function createSiteController(backendService) {
    const Dataset = backendService.getModel('Dataset');
    const Datapoint = backendService.getModel('Datapoint');
    let controller = {

    };
    return controller;
}

module.exports = createSiteController;